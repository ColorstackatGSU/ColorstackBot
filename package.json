{
  "name": "colorstack-gsu-bot",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP interactions-only Discord bot for ColorStack GSU verification flows.",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "register-commands": "node scripts/register-commands.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@vercel/functions": "^2.0.0",
    "discord-interactions": "^4.3.0",
    "dotenv": "^16.4.7",
    "googleapis": "^173.0.0"
  }
}
